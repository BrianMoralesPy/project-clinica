spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/clinica_dev
    username: postgres
    password: postgres

  jpa:
    show-sql: true

  flyway:
    locations: classpath:db/migration

logging:
  level:
    com.clinica: DEBUG
    org.hibernate.SQL: DEBUG
